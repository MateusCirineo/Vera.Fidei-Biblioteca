# Copie para .env no servidor e substitua todos os valores antes do deploy.
# O arquivo .env real e o backend/.env.production nunca devem entrar no Git.
POSTGRES_DB=vera_fidei
POSTGRES_USER=vera
POSTGRES_PASSWORD=troque-por-uma-senha-forte
VERA_PUBLIC_API_KEY=troque-pela-chave-publica-do-aplicativo
